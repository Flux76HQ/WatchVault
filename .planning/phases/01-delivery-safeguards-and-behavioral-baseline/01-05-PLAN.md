---
phase: 01-delivery-safeguards-and-behavioral-baseline
plan: "05"
type: execute
wave: 2
depends_on: ["01-02", "01-03"]
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vitest.config.ts
  - frontend/src/test/setup.ts
  - frontend/src/test/render.tsx
  - frontend/src/test/a11y.ts
  - frontend/tsconfig.node.json
autonomous: true
requirements: [DELV-05, DELV-06]
must_haves:
  truths:
    - "Exact approved Vitest, jsdom, Testing Library, axe, Playwright, Ajv, and YAML-parser pins are lockfile-backed."
    - "Frontend component tests have deterministic jsdom, provider rendering, user interaction, and axe helpers."
    - "Frontend package version remains synchronized at 1.0.1 after dependency installation."
  artifacts:
    - { path: frontend/vitest.config.ts, provides: "Vitest/jsdom configuration" }
    - { path: frontend/src/test/render.tsx, provides: "MemoryRouter/AppProvider render helper" }
    - { path: frontend/src/test/a11y.ts, provides: "axe-core helper" }
    - { path: frontend/package.json, provides: "Explicit test:contracts and validate:phase scripts with no broad test globs" }
  key_links:
    - { from: frontend/vitest.config.ts, to: frontend/src/test/setup.ts, via: "setupFiles" }
    - { from: frontend/package-lock.json, to: VERSION, via: "root version remains 1.0.1" }
    - { from: frontend/scripts/workflow-contract.test.mjs, to: frontend/node_modules/yaml, via: "frontend-local ESM package resolution after npm ci" }
  edge_dispositions:
    - "DELV-01 unclassified: package records remain exactly synchronized."
    - "DELV-02 unclassified: this protected change is covered by existing 1.0.1 bump."
    - "DELV-03 unclassified: bump remains a no-op after installation."
    - "DELV-04 unclassified: frontend tests stay out of pre-commit."
    - "DELV-05 adjacency: atomic IDs remain distinct."
    - "DELV-05 empty: invalid empty inventory fails."
    - "DELV-05 ordering: generated output is deterministic."
    - "DELV-06 unclassified: test runner is non-watch and CI-suitable."
  prohibitions:
    - { id: PROH-DELV-03-01, requirement_id: DELV-03, category: safety, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT create commits or tags, or modify files beyond VERSION and the two synchronized frontend manifest records." }
    - { id: PROH-DELV-05-01, requirement_id: DELV-05, category: product-integrity, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT omit, silently defer, or merge distinct current capabilities to manufacture acceptance coverage." }
    - { id: PROH-DELV-06-01, requirement_id: DELV-06, category: transparency, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT auto-accept visual baselines or treat unexplained skipped jobs as passing evidence." }
---

<objective>
Install the approved exact test stack and create reusable frontend test infrastructure.

Purpose: Supply the Wave 2 harness required by component, accessibility, inventory, and browser evidence.
Output: Locked dependencies, scripts, Vitest/jsdom config, and shared helpers.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<read_first>
- `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-02-SUMMARY.md` — mandatory package approvals.
- `frontend/package.json` and `frontend/package-lock.json` — synchronized 1.0.1 manifests.
- `frontend/vite.config.ts` and `frontend/tsconfig.node.json` — local config analogs.
- `frontend/src/main.tsx` and `frontend/src/lib/app.tsx` — provider composition and AppCtx contracts.
- `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-RESEARCH.md` — exact pins and setup contract.
</read_first>

<context>
@.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-03-SUMMARY.md
@.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install only approved exact test dependencies</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <action>After Plan 01-02 approval, install exactly Vitest 4.1.10, jsdom 29.1.1, Testing Library React 16.3.2/DOM 10.4.1/user-event 14.6.1/jest-dom 6.9.1, axe-core 4.12.1, Playwright Test 1.61.1, axe Playwright 4.12.1, Ajv 8.20.0, and approved `yaml` 2.8.1 as exact dev dependencies. Add non-watch `test`, `test:e2e`, and separately reviewable `test:e2e:update` scripts. Set `test:contracts` exactly to `node --test ../scripts/tests/version-policy.test.mjs ../scripts/tests/docker-contract.test.mjs ../scripts/tests/ci-changes.test.mjs scripts/workflow-contract.test.mjs scripts/capabilities.test.mjs`; do not use a broad filesystem glob. Set `validate:phase` exactly to `npm run test:contracts &amp;&amp; node ../scripts/version.mjs check &amp;&amp; node scripts/capabilities.mjs check &amp;&amp; python -m pytest ../backend/tests &amp;&amp; npm run build &amp;&amp; npm run test -- --run &amp;&amp; npm run test:e2e`. Keep the workflow contract under `frontend/scripts` so direct `yaml` resolution reaches this clean-installed dependency. Preserve package and lock root versions at 1.0.1.</action>
  <verify><automated>npm --prefix frontend ci &amp;&amp; npm --prefix frontend exec -- node --input-type=module -e "import('yaml').then(m=&gt;{if(typeof m.parseDocument!=='function')process.exit(1)})" &amp;&amp; npm pkg get scripts.test:contracts scripts.validate:phase --prefix frontend &amp;&amp; node scripts/version.mjs check</automated></verify>
  <acceptance_criteria>Clean install succeeds from lockfile; exact approved pins resolve; all canonical version records remain 1.0.1.</acceptance_criteria>
  <done>The approved dependency graph is reproducible and synchronized.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vitest and deterministic setup</name>
  <files>frontend/vitest.config.ts, frontend/src/test/setup.ts, frontend/tsconfig.node.json</files>
  <action>Configure React + jsdom through `vitest/config`, direct imports, setup cleanup, jest-dom/vitest assertions, cleared/restored mocks, and no watch default. Extend the node tsconfig include for test configs while retaining strict bundler resolution.</action>
  <verify><automated>npm --prefix frontend run test -- --run --passWithNoTests &amp;&amp; npm --prefix frontend run build</automated></verify>
  <acceptance_criteria>Vitest starts once under jsdom, exits, and the existing strict build still passes.</acceptance_criteria>
  <done>The frontend runner is CI-safe and deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Add provider-aware render and axe helpers</name>
  <files>frontend/src/test/render.tsx, frontend/src/test/a11y.ts</files>
  <action>Compose MemoryRouter and the real AppProvider boundary in a controllable render helper matching `main.tsx`; expose route/initial-entry controls without introducing a barrel. Wrap axe-core in a typed async helper that fails on violations and receives rendered containers. Keep all identities/preferences synthetic.</action>
  <verify><automated>npm --prefix frontend run test -- --run --passWithNoTests &amp;&amp; npm --prefix frontend run build</automated></verify>
  <acceptance_criteria>Helpers type-check, import directly, use existing provider boundaries, and contain no live credentials or network dependency.</acceptance_criteria>
  <done>Component plans can test behavior and accessibility without duplicating setup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| npm registry → lockfile | Approved third-party code enters development/CI. |
| synthetic test state → DOM/artifacts | Test identities and failures may be serialized. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | dependencies | high | mitigate | Exact pins, committed lockfile, `npm ci`, prior human legitimacy approval, and frontend-local yaml resolution. |
| T-01-02 | Disclosure | test state | high | mitigate | Synthetic users only; no environment dumps or real cookies. |
| T-01-03 | Elevation | browser packages | high | mitigate | Dev dependencies only; pinned Chromium installed later, no install hooks. |
| T-01-04 | Tampering | version mirrors | high | mitigate | `node scripts/version.mjs check` after lockfile mutation. |
| T-01-05 | Tampering | refs/paths | high | mitigate | Version engine rejects unsafe Git inputs and uses argument arrays. |
| T-01-06 | Elevation/Disclosure | PR boundary | high | mitigate | Plan 01-09 uses read-only PR jobs and no privileged trigger. |
| T-01-07 | Tampering | generated report | high | mitigate | Plan 01-07 validates canonical JSON and byte freshness. |
| T-01-SC | Tampering | npm installs | high | mitigate | Plan 01-02 is a completed blocking human-action prerequisite. |
</threat_model>

<verification>
Focused checks: `npm --prefix frontend ci && npm --prefix frontend run build && npm --prefix frontend run test -- --run --passWithNoTests && node scripts/version.mjs check`; inspect the two explicit package scripts and direct yaml import as in Task 1 without executing later RED contracts. Final phase gate after Wave 5, in the pinned CI visual environment: `npm --prefix frontend run validate:phase`.
</verification>

<success_criteria>
- Exact dependency pins and lockfile are reproducible.
- Vitest/jsdom/Testing Library/axe helpers are ready.
- Version remains 1.0.1; no UI redesign occurs.
</success_criteria>

<output>
Create `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces
- Locked frontend test dependencies and scripts
- `frontend/vitest.config.ts`
- `frontend/src/test/{setup,render,a11y}.ts(x)`
