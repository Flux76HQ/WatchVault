---
phase: 01-delivery-safeguards-and-behavioral-baseline
plan: "06"
type: execute
wave: 3
depends_on: ["01-05"]
files_modified:
  - frontend/src/App.test.tsx
  - frontend/src/pages/Settings.test.tsx
  - frontend/src/pages/TitleDetail.test.tsx
autonomous: true
requirements: [DELV-06]
must_haves:
  truths:
    - "Auth gates, theme/preferences, semantic states, permission gates, and one watch mutation have component-level regression evidence."
    - "Representative login, authenticated shell, settings, and action DOMs have axe-core evidence."
  artifacts:
    - { path: frontend/src/App.test.tsx, provides: "Auth/routing/loading regression evidence" }
    - { path: frontend/src/pages/Settings.test.tsx, provides: "Theme/preference behavior and accessibility evidence" }
    - { path: frontend/src/pages/TitleDetail.test.tsx, provides: "Permission/mutation/error evidence" }
  key_links:
    - { from: frontend/src/App.test.tsx, to: frontend/src/test/render.tsx, via: "provider-aware MemoryRouter render" }
    - { from: frontend/src/pages/TitleDetail.test.tsx, to: frontend/src/lib/api.ts, via: "mock at existing API boundary" }
  edge_dispositions:
    - "DELV-01 unclassified: test changes retain synchronized 1.0.1."
    - "DELV-02 unclassified: protected test changes remain covered by the aggregate bump."
    - "DELV-03 unclassified: bump stays a no-op."
    - "DELV-04 unclassified: component checks remain CI-only."
    - "DELV-05 adjacency: distinct route/action/state obligations remain distinct inventory items."
    - "DELV-05 empty: absent fixture states are explicit failures, not silent success."
    - "DELV-05 ordering: equal-result queries use semantic identity, not incidental DOM order."
    - "DELV-06 unclassified: deterministic component or axe failure is blocking when selected."
  prohibitions:
    - { id: PROH-DELV-03-01, requirement_id: DELV-03, category: safety, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT create commits or tags, or modify files beyond VERSION and the two synchronized frontend manifest records." }
    - { id: PROH-DELV-05-01, requirement_id: DELV-05, category: product-integrity, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT omit, silently defer, or merge distinct current capabilities to manufacture acceptance coverage." }
    - { id: PROH-DELV-06-01, requirement_id: DELV-06, category: transparency, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT auto-accept visual baselines or treat unexplained skipped jobs as passing evidence." }
---

<objective>
Establish representative frontend behavior and DOM-accessibility regression evidence.

Purpose: Fulfill D-15/D-19 component coverage without redesigning product UI.
Output: Focused Vitest/Testing Library/user-event/axe suites.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<read_first>
- `frontend/src/App.tsx` — loading/login/authenticated route gates.
- `frontend/src/lib/app.tsx` — theme, preferences, scope, and permissions.
- `frontend/src/pages/Settings.tsx` — preference behavior to preserve.
- `frontend/src/pages/TitleDetail.tsx` — existing `ingest.write` watch mutation.
- `frontend/src/test/render.tsx` and `frontend/src/test/a11y.ts` — required shared helpers.
- `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-PATTERNS.md` — no-analog component/accessibility test conventions.
- `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-RESEARCH.md` — cited Vitest/setup/a11y patterns and D-15 boundaries.
</read_first>

<context>
@.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-05-SUMMARY.md
@.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover authentication, routing, themes, and preferences</name>
  <files>frontend/src/App.test.tsx, frontend/src/pages/Settings.test.tsx</files>
  <behavior>
    - Loading, unauthenticated Login, authenticated index/route, and wildcard fallback render the existing destinations.
    - Light/dark/system and saved preference changes apply through AppProvider behavior.
    - Semantic queries and keyboard interaction succeed; representative DOMs have no axe violations.
  </behavior>
  <action>Use the shared render helper, deterministic API/provider fakes, semantic Testing Library queries, and `userEvent.setup()`. Because PATTERNS.md records no local frontend-test or axe analog, follow its explicit direction to use the cited RESEARCH.md Vitest setup and axe helper patterns rather than inventing a precedent. Exercise D-15 while preserving current route and preference behavior. Include loading, empty, error, disabled/busy, and success feedback where the existing components expose them; do not alter product TSX/CSS.</action>
  <verify><automated>npm --prefix frontend run test -- --run src/App.test.tsx src/pages/Settings.test.tsx</automated></verify>
  <acceptance_criteria>Auth gates and theme/preference behavior pass under jsdom; login, shell, and settings samples have zero axe violations.</acceptance_criteria>
  <done>Core route and preference behavior has component regression evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover permission-gated watch mutation and feedback</name>
  <files>frontend/src/pages/TitleDetail.test.tsx</files>
  <behavior>
    - Without `ingest.write`, the mutation is unavailable.
    - Authorized keyboard interaction posts the exact scoped date payload once.
    - Pending, success/toast-refresh, and ApiError feedback remain observable and accessible.
  </behavior>
  <action>Mock only `frontend/src/lib/api.ts` at its existing boundary, use synthetic title/profile data, and test the real TitleDetail interaction per D-15. Apply the PATTERNS.md no-analog rule by following the RESEARCH.md component/accessibility contract. Assert request method/path/body and forbidden side effects; run the shared axe helper on idle and action states.</action>
  <verify><automated>npm --prefix frontend run test -- --run src/pages/TitleDetail.test.tsx &amp;&amp; npm --prefix frontend run build</automated></verify>
  <acceptance_criteria>Permission denial, exact mutation, reconciliation trigger, and error behavior pass with no live network/database and no product source changes.</acceptance_criteria>
  <done>One critical mutation has deterministic component and accessibility evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Synthetic API data → React state | Fixture permissions, identities, and errors cross the client boundary. |
| Test failures → CI artifacts | Rendered output may be retained on failure. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Spoofing | fixture users | high | mitigate | Synthetic fixed users/permissions only; tests exercise the real `can` gate. |
| T-01-02 | Disclosure | test output | high | mitigate | No real cookies, tokens, credentials, or environment dumps. |
| T-01-03 | Tampering | API mocks | medium | mitigate | Mock only the existing API boundary and assert exact method/path/body. |
| T-01-04 | Tampering/Elevation | PR jobs | high | mitigate | Plan 01-09 uses read-only `pull_request` jobs and no privileged trigger. |
| T-01-05 | Tampering | refs/paths | high | mitigate | Shared version engine rejects unsafe paths and shell interpolation. |
| T-01-06 | Tampering | generated inventory | high | mitigate | Plan 01-07 checks schema/discovery/report freshness. |
| T-01-SC | Tampering | dependencies | high | mitigate | Exact approved lockfile from Plan 01-05. |
</threat_model>

<verification>
Focused checks: `npm --prefix frontend run build && npm --prefix frontend run test -- --run`. Final phase gate after Wave 5: `npm --prefix frontend run validate:phase`.
</verification>

<success_criteria>
- Representative behavior, keyboard interaction, and axe checks pass.
- Tests use synthetic deterministic boundaries.
- No product UI redesign is introduced.
</success_criteria>

<output>
Create `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-06-SUMMARY.md` when done.
</output>

## Artifacts this phase produces
- `frontend/src/App.test.tsx`
- `frontend/src/pages/Settings.test.tsx`
- `frontend/src/pages/TitleDetail.test.tsx`
