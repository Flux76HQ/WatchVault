---
phase: 01-delivery-safeguards-and-behavioral-baseline
plan: "08"
type: execute
wave: 3
depends_on: ["01-05"]
files_modified:
  - frontend/playwright.config.ts
  - frontend/e2e/fixtures/data.ts
  - frontend/e2e/fixtures/api.ts
  - frontend/e2e/reference-journey.spec.ts
  - .gitignore
autonomous: true
requirements: [DELV-06]
must_haves:
  truths:
    - "Pinned Chromium runs desktop/mobile × dark/light against the real React routes."
    - "A fail-closed stateful browser API fixture verifies dashboard → search → title watch mutation → refreshed dashboard."
    - "Four checkpoints in four projects are generation-ready for the authoritative pinned Linux CI route and include axe evidence."
  artifacts:
    - { path: frontend/playwright.config.ts, provides: "Four pinned Chromium projects" }
    - { path: frontend/e2e/fixtures/api.ts, provides: "Strict stateful API boundary" }
    - { path: frontend/e2e/reference-journey.spec.ts, provides: "Reference journey, axe, and visual assertions" }
  key_links:
    - { from: frontend/e2e/fixtures/api.ts, to: frontend/src/lib/api.ts, via: "same-origin /api method/body contract" }
    - { from: frontend/e2e/reference-journey.spec.ts, to: frontend/e2e/fixtures/api.ts, via: "route interception before navigation" }
    - { from: .github/workflows/ci.yml, to: frontend/e2e/reference-journey.spec.ts, via: "ordinary comparison and baseline generation both run in mcr.microsoft.com/playwright:v1.61.1-noble" }
  edge_dispositions:
    - "DELV-01 unclassified: browser changes remain under synchronized 1.0.1."
    - "DELV-02 unclassified: browser fixture paths are version-protected."
    - "DELV-03 unclassified: existing sufficient bump remains a no-op."
    - "DELV-04 unclassified: browser checks remain CI-only."
    - "DELV-05 adjacency: four journey states remain distinct evidence checkpoints."
    - "DELV-05 empty: missing/null fixture response fails closed."
    - "DELV-05 ordering: fixed IDs/time and ordered checkpoints make output stable."
    - "DELV-06 unclassified: any unknown request, axe issue, visual diff, or journey failure blocks selected browser evidence."
  prohibitions:
    - { id: PROH-DELV-03-01, requirement_id: DELV-03, category: safety, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT create commits or tags, or modify files beyond VERSION and the two synchronized frontend manifest records." }
    - { id: PROH-DELV-05-01, requirement_id: DELV-05, category: product-integrity, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT omit, silently defer, or merge distinct current capabilities to manufacture acceptance coverage." }
    - { id: PROH-DELV-06-01, requirement_id: DELV-06, category: transparency, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT auto-accept visual baselines or treat unexplained skipped jobs as passing evidence." }
---

<objective>
Create deterministic Chromium journey, accessibility, and visual regression evidence.

Purpose: Implement D-16/D-17 against current behavior without creating a new history route or redesigning UI.
Output: Four projects, strict browser fixtures, one stateful journey, and 16 snapshot candidates.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<read_first>
- `frontend/src/lib/api.ts` and `frontend/src/lib/app.tsx` — API/bootstrap contracts.
- `frontend/src/pages/Dashboard.tsx`, `Search.tsx`, and `TitleDetail.tsx` — exact journey calls.
- `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-RESEARCH.md` — four projects, fixed dimensions, and current-history behavior.
- `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-PATTERNS.md` — fixture and visual conventions.
</read_first>

<context>
@.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-05-SUMMARY.md
@.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure pinned deterministic Chromium projects</name>
  <files>frontend/playwright.config.ts, .gitignore</files>
  <action>Configure Chromium-only projects `chromium-desktop-dark`, `chromium-desktop-light`, `chromium-mobile-dark`, and `chromium-mobile-light` at 1440×900 and 390×844, with fixed locale/timezone/time, touch/mobile emulation, disabled animation, CSS-scale screenshots, `maxDiffPixelRatio: 0.001`, retries/artifacts only as researched, and a Vite web server. Ignore transient report/test-result/trace outputs but not committed baselines. Install only the exact Chromium revision for Playwright 1.61.1. Keep focused local feedback nonvisual; Plan 01-09 must run ordinary four-project visual comparison in the same `mcr.microsoft.com/playwright:v1.61.1-noble` environment as authoritative baseline generation.</action>
  <verify><automated>npm --prefix frontend exec playwright install chromium &amp;&amp; npm --prefix frontend exec playwright test -- --list</automated></verify>
  <acceptance_criteria>Exactly four named Chromium projects list the reference suite and transient outputs are ignored.</acceptance_criteria>
  <done>The browser matrix is pinned and deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build a strict stateful synthetic API fixture</name>
  <files>frontend/e2e/fixtures/data.ts, frontend/e2e/fixtures/api.ts</files>
  <behavior>
    - Auth/profile/preferences/dashboard/search/title calls return backend-shaped fixed data.
    - Exact add-watch POST mutates in-memory history and subsequent recent data.
    - Unknown API method/path/body and external asset requests are recorded, aborted, and fail teardown.
  </behavior>
  <action>Install route interception before first navigation per D-16. Use fixed synthetic IDs, permissions, dates, local assets, and no credentials. Mirror real response shapes and cookie/same-origin semantics; do not return generic success. Expose teardown assertion for zero unhandled requests.</action>
  <verify><automated>npm --prefix frontend run build &amp;&amp; npm --prefix frontend exec playwright test -- --project=chromium-desktop-dark --grep="fixture contract"</automated></verify>
  <acceptance_criteria>Known calls pass, exact mutation changes state, and deliberately unknown calls fail closed without external traffic.</acceptance_criteria>
  <done>The real React client is isolated by a strict deterministic browser boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the blocking reference journey contract</name>
  <files>frontend/e2e/reference-journey.spec.ts</files>
  <behavior>
    - Dashboard renders, search opens fixed title, authorized add-watch sends exact body, and dashboard reflects the new record.
    - Axe scan passes at dashboard, search, title, and post-mutation history checkpoints.
    - Four projects each capture four project-qualified screenshots.
  </behavior>
  <action>Implement D-17 on the current dashboard→search→title→watch mutation→refreshed dashboard flow; do not invent a history page. Assert keyboard-reachable controls, exact API state transition, zero unhandled requests, four checkpoint names, and visual assertions without updating or committing baselines locally. Mark a nonvisual fixture/journey path `@smoke` so task feedback can run one desktop project quickly. Plan 01-09 owns the pinned Linux generation route and Plan 01-10 owns artifact review/commitment.</action>
  <verify><automated>npm --prefix frontend exec playwright test -- --project=chromium-desktop-dark --grep="@smoke" --reporter=line</automated></verify>
  <acceptance_criteria>The focused desktop smoke proves the journey and fixture contract; configuration statically declares four projects × four visual checkpoints for later Linux generation.</acceptance_criteria>
  <done>Current reference behavior is ready for authoritative Linux generation without assuming local Docker or accepting snapshots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser requests → fixture router | Request URLs, bodies, cookies, and assets cross a synthetic trust boundary. |
| Browser failures → retained artifacts | Screenshots/traces/reports may disclose fixture state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Spoofing | fixture identity | high | mitigate | Fixed synthetic identity/permissions only; no real sessions. |
| T-01-02 | Disclosure | traces/screenshots | high | mitigate | Failure-only artifacts, synthetic data, no env dumps, 14-day CI retention. |
| T-01-03 | Tampering | permissive routes | high | mitigate | Exact method/path/body dispatch; unknown/external requests abort and fail teardown. |
| T-01-04 | Tampering | visual baselines | high | mitigate | Separate update command, identical pinned Linux/Chromium environments for normal comparison and generation, and Plan 01-10 human review; ordinary CI cannot accept. |
| T-01-05 | Elevation/Disclosure | PR workflow | high | mitigate | Plan 01-09 uses read-only PR permissions and no secrets/`pull_request_target`. |
| T-01-06 | Tampering | refs/paths | high | mitigate | Shared normalized policy and argument-array Git commands. |
| T-01-07 | Tampering | generated inventory | high | mitigate | Plan 01-07 enforces canonical JSON/report freshness. |
| T-01-SC | Tampering | browser dependency | high | mitigate | Exact approved Playwright package and pinned Chromium only. |
</threat_model>

<verification>
Focused checks: `npm --prefix frontend run build && npm --prefix frontend exec playwright test -- --list && npm --prefix frontend exec playwright test -- --project=chromium-desktop-dark --grep="@smoke" --reporter=line`. The full four-project comparison is reserved for the pinned Linux/Chromium Phase 1 gate after Plan 01-10 installs the approved baselines: `npm --prefix frontend run validate:phase`.
</verification>

<success_criteria>
- Four desktop/mobile × dark/light projects pass.
- Stateful fixture fails closed and contains no credentials.
- The 16-snapshot matrix is fully specified and cannot be generated or accepted by ordinary local validation.
</success_criteria>

<output>
Create `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-08-SUMMARY.md` when done.
</output>

## Artifacts this phase produces
- `frontend/playwright.config.ts`
- strict browser fixtures
- reference journey with axe checks
- generation-ready four-project visual contract
