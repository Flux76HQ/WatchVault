---
phase: 01-delivery-safeguards-and-behavioral-baseline
plan: "02"
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: false
requirements: [DELV-05, DELV-06]
must_haves:
  truths:
    - "Every research-flagged npm package receives explicit human legitimacy approval before installation."
  artifacts:
    - path: .planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-02-SUMMARY.md
      provides: "Audit record of exact package identities, pins, repositories, and administrator approval or rejection"
  key_links:
    - from: "01-RESEARCH.md Package Legitimacy Audit"
      to: "frontend/package-lock.json"
      via: "blocking approval before exact installation"
  edge_dispositions:
    - "DELV-01 unclassified: stable X.Y.Z and exact mirror/tag equality are required."
    - "DELV-02 unclassified: protected aggregate diffs require a greater version and exact remediation."
    - "DELV-03 unclassified: no-change and already-sufficient inputs are no-ops; patch/minor/major are exact."
    - "DELV-04 unclassified: hooks inspect staged state; CI is authoritative."
    - "DELV-05 adjacency: duplicate IDs collide; distinct atomic IDs remain separate."
    - "DELV-05 empty: null/empty fail; singleton still must satisfy full discovery."
    - "DELV-05 ordering: deterministic phase/requirement/kind/ID order."
    - "DELV-06 unclassified: selected failure blocks; verified unselected skip is neutral."
  prohibitions:
    - { id: PROH-DELV-03-01, requirement_id: DELV-03, category: safety, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT create commits or tags, or modify files beyond VERSION and the two synchronized frontend manifest records." }
    - { id: PROH-DELV-05-01, requirement_id: DELV-05, category: product-integrity, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT omit, silently defer, or merge distinct current capabilities to manufacture acceptance coverage." }
    - { id: PROH-DELV-06-01, requirement_id: DELV-06, category: transparency, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT auto-accept visual baselines or treat unexplained skipped jobs as passing evidence." }
---

<objective>
Clear the mandatory supply-chain legitimacy gate for the exact frontend test stack.

Purpose: Prevent flagged packages from being installed without human verification.
Output: Approval or a blocking rejection for the four SUS packages.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<read_first>
- `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-RESEARCH.md` — Package Legitimacy Audit and exact pins.
- `frontend/package.json` — current dependency baseline and Node-compatible tooling.
</read_first>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Verify flagged package identities and pins</name>
  <files>None (verification-only checkpoint)</files>
  <action>Review the four exact registry identities and pins below; do not approve by package-name familiarity alone, and do not install anything in this plan.</action>
  <what-built>No packages have been installed. Research flagged four mature official packages solely because their latest releases were under the age threshold.</what-built>
  <how-to-verify>
1. Verify `vitest@4.1.10` at https://www.npmjs.com/package/vitest and its repository `vitest-dev/vitest`.
2. Verify `@testing-library/jest-dom@6.9.1` at https://www.npmjs.com/package/@testing-library/jest-dom and confirm this Node-20-compatible pin, not 7.x.
3. Verify `@playwright/test@1.61.1` at https://www.npmjs.com/package/@playwright/test and its repository `microsoft/playwright`.
4. Verify `@axe-core/playwright@4.12.1` at https://www.npmjs.com/package/@axe-core/playwright and its repository `dequelabs/axe-core-npm`.
5. Reject approval if package owner, repository, exact version, or expected purpose differs from `01-RESEARCH.md`.
  </how-to-verify>
  <verify>
    <automated>node -e "const {execFileSync}=require('node:child_process');const npm=process.platform==='win32'?'npm.cmd':'npm';for(const p of ['vitest@4.1.10','@testing-library/jest-dom@6.9.1','@playwright/test@1.61.1','@axe-core/playwright@4.12.1'])execFileSync(npm,['view',p,'version','repository.url','engines','--json'],{stdio:'inherit'});"</automated>
  </verify>
  <acceptance_criteria>All four exact package/version/repository triples match the research audit; any mismatch blocks Plan 01-05.</acceptance_criteria>
  <done>All four exact pins are explicitly approved, or execution remains blocked with the rejected package identified.</done>
  <resume-signal>Type "approved" only after all four identities and exact pins are confirmed, or describe the rejected package.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| npm registry metadata → repository lockfile | Third-party package identity and release metadata cross into trusted build inputs. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | Package identity | high | mitigate | Human verifies official registry/repository identity and exact versions. |
| T-01-02 | Elevation | Install scripts | high | mitigate | Audit confirms no registry postinstall; installation stays blocked until approval. |
| T-01-03 | Disclosure | PR/package jobs | high | mitigate | Later workflow uses read-only PR permissions and no secrets. |
| T-01-04 | Tampering | Git refs/paths | high | mitigate | Version tooling later rejects unsafe refs/paths and uses argument arrays. |
| T-01-05 | Tampering | Generated files | high | mitigate | Inventory freshness checks later fail on drift. |
| T-01-SC | Tampering | npm installs | high | mitigate | This non-auto-approvable checkpoint is the blocking control. |
</threat_model>

<verification>
Do not install or modify package files in this plan. Final phase gate after Wave 5: `npm --prefix frontend run validate:phase`; it is not run at this approval-only checkpoint.
</verification>

<success_criteria>
- All four SUS packages are explicitly approved at their exact pins.
- No package installation occurs before approval.
</success_criteria>

<output>
Create `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces
- Human legitimacy approval recorded in `01-02-SUMMARY.md`
