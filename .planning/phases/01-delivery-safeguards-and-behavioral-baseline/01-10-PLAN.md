---
phase: 01-delivery-safeguards-and-behavioral-baseline
plan: "10"
type: execute
wave: 5
depends_on: ["01-09"]
files_modified:
  - frontend/e2e/reference-journey.spec.ts-snapshots/**
autonomous: false
requirements: [DELV-01, DELV-02, DELV-03, DELV-04, DELV-05, DELV-06]
user_setup:
  - service: github
    why: "Make the repository-external stable aggregate status a required main-branch merge check."
    dashboard_config:
      - task: "Require exact status check `delivery / gate` after it has appeared in a workflow run."
        location: "GitHub repository Settings → Rules → Rulesets → main branch ruleset"
must_haves:
  truths:
    - "All 16 desktop/mobile × dark/light baselines are intentionally approved."
    - "The live main-branch ruleset requires exact check `delivery / gate` with no bypass."
    - "The documented full suite passes and a second bump leaves version 1.0.1 unchanged."
  artifacts:
    - path: frontend/e2e/reference-journey.spec.ts-snapshots/**
      provides: "Exactly 16 authoritative Linux/Chromium baseline PNGs downloaded from the reviewed CI artifact"
    - path: .planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-10-SUMMARY.md
      provides: "Snapshot hash/manifest approval, full-suite result, capability review, and live ruleset audit record"
    - external: "GitHub main-branch ruleset"
      provides: "Pull-request requirement and exact required status `delivery / gate` with no bypass"
  key_links:
    - { from: "GitHub main ruleset", to: ".github/workflows/ci.yml", via: "required status context `delivery / gate`" }
    - { from: "human baseline approval", to: "frontend/e2e/reference-journey.spec.ts-snapshots/**", via: "review of all 16 images" }
  edge_dispositions:
    - "DELV-01 unclassified: full gate proves exact canonical/mirror/tag contract."
    - "DELV-02 unclassified: protected failure and exact remediation evidence is green."
    - "DELV-03 unclassified: second bump is byte-preserving at 1.0.1."
    - "DELV-04 unclassified: configured hook is fast and CI-required."
    - "DELV-05 adjacency: duplicate collision and distinct atomic entries are reviewed."
    - "DELV-05 empty: null/empty rejection and full discovery equality are green."
    - "DELV-05 ordering: generated report is byte-stable."
    - "DELV-06 unclassified: selected failures block the required aggregate status."
  prohibitions:
    - { id: PROH-DELV-03-01, requirement_id: DELV-03, category: safety, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT create commits or tags, or modify files beyond VERSION and the two synchronized frontend manifest records." }
    - { id: PROH-DELV-05-01, requirement_id: DELV-05, category: product-integrity, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT omit, silently defer, or merge distinct current capabilities to manufacture acceptance coverage." }
    - { id: PROH-DELV-06-01, requirement_id: DELV-06, category: transparency, status: unresolved, verification: null, flagged_unverified: true, statement: "MUST NOT auto-accept visual baselines or treat unexplained skipped jobs as passing evidence." }
---

<objective>
Complete the two external human verifications that automated repository evidence cannot self-approve.

Purpose: Make visual intent and merge enforcement authoritative before redesign work begins.
Output: Approved 16-image baseline, required live aggregate check, and final full-suite evidence.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<read_first>
- `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-08-SUMMARY.md` — four-project journey and fixture contract.
- `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-09-SUMMARY.md` — final workflow/check identity.
- `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-VALIDATION.md` — manual-only and full-suite contract.
- `docs/CAPABILITIES.md` — generated parity review.
</read_first>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrieve and review authoritative Linux baselines</name>
  <files>frontend/e2e/reference-journey.spec.ts-snapshots/**</files>
  <action>Trigger or select the successful `workflow_dispatch` baseline-generation run from Plan 01-09, use `gh run download` to retrieve its artifact, and reject any artifact whose manifest commit SHA does not match the reviewed revision, whose package/browser/container identity differs from Playwright 1.61.1 plus `mcr.microsoft.com/playwright:v1.61.1-noble`, whose SHA-256 entries do not match, or whose PNG count is not exactly 16. Copy the verified PNG set into the canonical snapshot directory without running the update command locally. Record artifact run ID, manifest values, and hashes in `01-10-SUMMARY.md`; this is candidate commitment, not visual approval.</action>
  <verify>
    <automated>npm --prefix frontend exec playwright test -- --project=chromium-desktop-dark --grep="@smoke" --reporter=line</automated>
    <human-check>Open all 16 canonical PNGs; confirm every project has dashboard, search, title-detail, and post-mutation checkpoints; confirm correct theme/viewport, visible mutation, no blank/clipped/credential-bearing content, no redesign, and no unexplained image. Approve only this manifest-backed set.</human-check>
  </verify>
  <acceptance_criteria>Exactly 16 manifest-backed images are reviewed and the focused smoke passes without updating them; the ordinary four-project comparison remains the phase-level gate below.</acceptance_criteria>
  <done>All 16 images are explicitly approved, or rejected images are named and the phase remains blocked.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Configure the required aggregate gate</name>
  <files>None (verification-only checkpoint)</files>
  <action>A repository administrator must configure the repository-external ruleset after `delivery / gate` has appeared in a real run. Require pull requests and exact status `delivery / gate` on `main`, remove bypass permission, then verify a selected failing child blocks merge. The execution agent may inspect the result but must not substitute API automation for the administrator action. Review `docs/CAPABILITIES.md` and report any omitted current behavior.</action>
  <what-built>CI always emits `delivery / gate`; selected failures block it; Docker publication depends on it; local hook remains fast.</what-built>
  <how-to-verify>
1. After `delivery / gate` has appeared in a real workflow run, inspect the main-branch ruleset.
2. Confirm pull requests and exact required status `delivery / gate` are enabled and bypass is not permitted.
3. Inspect a run with a selected failing child job, or a safe test PR, and confirm merge is blocked.
4. Review `docs/CAPABILITIES.md` for explicit ownership and report any omitted current behavior rather than approving.
  </how-to-verify>
  <verify>
    <automated>node scripts/version.mjs check &amp;&amp; node frontend/scripts/capabilities.mjs check &amp;&amp; gh api repos/Flux76HQ/WatchVault/rulesets</automated>
  </verify>
  <acceptance_criteria>The live ruleset requires exact `delivery / gate` with no bypass, a selected failure blocks merge, and capability review reports no omitted current behavior.</acceptance_criteria>
  <done>The exact aggregate check is required without bypass and all DELV-01 through DELV-06 evidence is approved.</done>
  <resume-signal>Type "configured" after the administrator action and external verification pass, or report the exact failed criterion.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CI status → branch ruleset | Repository-external enforcement must bind the exact stable context. |
| Visual artifacts → human approval | Generated images could conceal leaks or unintended baseline changes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | required check | high | mitigate | Human confirms exact context, PR requirement, blocked failing child, and no bypass. |
| T-01-02 | Tampering | visual baselines | high | mitigate | Review all 16 images; ordinary E2E cannot update them. |
| T-01-03 | Disclosure | snapshots/artifacts | high | mitigate | Reject any credential/environment/user data; fixtures must remain synthetic. |
| T-01-04 | Elevation | PR boundary | high | mitigate | Verify read-only PR workflow and absence of privileged target trigger. |
| T-01-05 | Tampering | refs/paths | high | mitigate | Final version tests cover normalized/rejected unsafe inputs and argument arrays. |
| T-01-06 | Tampering | generated report | high | mitigate | Machine freshness plus human omission/ownership review. |
| T-01-SC | Tampering | dependencies | high | mitigate | Final clean install/full suite uses exact approved lockfile and pinned Chromium. |
</threat_model>

<verification>
Phase gate, executed by CI in `mcr.microsoft.com/playwright:v1.61.1-noble` after explicit Python 3.12/backend-requirements and Node 20.19.x/frontend-lockfile provisioning: `npm --prefix frontend run validate:phase`. Then run the hash-based second-bump no-op test from Plan 01-03 and verify the live ruleset. Keep this full four-project suite at plan/phase validation rather than task feedback.
</verification>

<success_criteria>
- DELV-01 through DELV-06 are green and externally enforceable.
- All D-01 through D-19 locked decisions are evidenced.
- All eight edge rows and three unresolved descriptor-less prohibitions remain visible; none is silently dropped.
- Visual redesign work may begin without weakening current capabilities.
</success_criteria>

<output>
Create `.planning/phases/01-delivery-safeguards-and-behavioral-baseline/01-10-SUMMARY.md` when done.
</output>

## Artifacts this phase produces
- Approved 16-image Chromium baseline
- Required live `delivery / gate` main-branch rule
- Final full-suite and capability-coverage evidence
